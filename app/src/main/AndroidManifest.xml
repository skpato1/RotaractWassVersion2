<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rotaract.ayedi.wass.rotaractwassversion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RotaractClubs.PaysActivity" />
        <activity android:name=".RotaractClubs.PaysAmeriqueDeSudActivity" />
        <activity android:name=".RotaractClubs.PaysAmeriqueDeNordActivity" />
        <activity android:name=".RotaractClubs.PaysAsieActivity" />
        <activity android:name=".RotaractClubs.PaysEuropeActivity" />
        <activity android:name=".RotaractClubs.PaysOceanieActivity" />
        <activity android:name=".RotaractClubs.PaysAfriqueActivity" />
        <activity android:name=".RotaractClubs.RotaractAfriqueActivity" />
        <activity android:name=".database.CopyDbActivity"></activity>
    </application>

</manifest>